# =============================================================================
# Substream Platform — Environment Configuration
# =============================================================================
#
# Copy to `.env` and fill in values. See DEPLOYMENT.md for full guidance.
#
#   cp env.example.txt .env
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (required)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:password@localhost:5432/substream

# -----------------------------------------------------------------------------
# REDIS (required in production)
# -----------------------------------------------------------------------------
# Rate limiting, viewer counts, reactions, stage pool, webhook queue.
# Falls back to in-memory if unset (dev only — breaks multi-instance).
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# AWS CREDENTIALS (required for IVS)
# -----------------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Used to construct HLS playback URLs for IVS Real-Time stages.
AWS_ACCOUNT_ID=

# -----------------------------------------------------------------------------
# IVS REAL-TIME CONFIGURATION (required for WebRTC streaming)
# -----------------------------------------------------------------------------
# Run `pnpm ivs:setup` to create these resources automatically.
IVS_STAGE_ARN=
IVS_CHANNEL_ARN=
# Optional: S3 recording
IVS_STORAGE_ARN=
IVS_S3_BUCKET=

# -----------------------------------------------------------------------------
# IVS LOW-LATENCY CONFIGURATION (optional — legacy RTMPS)
# -----------------------------------------------------------------------------
IVS_RECORDING_CONFIG_ARN=
IVS_PLAYBACK_KEY_PAIR_ID=
IVS_PLAYBACK_PRIVATE_KEY=

# S3 bucket where IVS writes recordings. Required for VOD URLs on end-of-stream.
S3_RECORDING_BUCKET=

# -----------------------------------------------------------------------------
# ENCRYPTION & AUTH (required for production)
# -----------------------------------------------------------------------------
# Generate both with: openssl rand -hex 64  (or -hex 32 for the 32-byte one)
STREAM_KEY_ENCRYPTION_KEY=
JWT_SECRET=

# -----------------------------------------------------------------------------
# HIGHLIGHT SERVICE (required for AI highlight generation)
# -----------------------------------------------------------------------------
# URL of the highlight-service (FastAPI, in ./highlight-service). If unset,
# highlight creation will store a DB row but will not trigger the AI pipeline.
HIGHLIGHT_SERVICE_URL=http://localhost:8080

# Shared HMAC secret for signing highlight-service → platform callbacks.
# Generate with: openssl rand -hex 32
HIGHLIGHT_CALLBACK_SECRET=

# Public-facing base URL for this platform — used as the callback host sent to
# the highlight-service. Must be reachable from the highlight-service.
PUBLIC_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# DEMO / DASHBOARD (optional)
# -----------------------------------------------------------------------------
# Access code for org slug login at /login (defaults to 'livewave123').
# DEMO_ORG_CODE=livewave123

# -----------------------------------------------------------------------------
# MARKETING / SEO (optional)
# -----------------------------------------------------------------------------
# Used for OG metadata in src/app/layout.tsx. Defaults to the demo domain.
# NEXT_PUBLIC_SITE_URL=https://demo.substream.dev

# -----------------------------------------------------------------------------
# PRODUCT ANALYTICS (optional — PostHog)
# -----------------------------------------------------------------------------
# When set, the client initializes posthog-js in instrumentation-client.ts
# and the server captures events via src/lib/posthog-server.ts. Leave unset
# to disable analytics entirely.
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# -----------------------------------------------------------------------------
# STAGE POOL (optional — pre-allocated WebRTC stages for fast stream start)
# -----------------------------------------------------------------------------
# STAGE_POOL_SIZE=3
# STAGE_POOL_MIN_AVAILABLE=1

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# LOG_LEVEL=info

# -----------------------------------------------------------------------------
# CLOUDFRONT (optional — VOD delivery)
# -----------------------------------------------------------------------------
# CLOUDFRONT_KEY_PAIR_ID=
# CLOUDFRONT_PRIVATE_KEY=

NODE_ENV=development
