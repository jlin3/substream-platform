# Nixpacks configuration for IVS Streaming Backend

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["npx prisma generate && pnpm build"]

[start]
cmd = "npx prisma migrate deploy && pnpm start"
