---
description: Enforce correct GitHub remote for all git push operations
alwaysApply: true
---

# Git Remote Policy

- Never push to the `dreamtreelab` GitHub organization.
- The only valid remote is `origin` pointing to `github.com/jlin3/substream-platform`.
- Before any `git push`, verify `git remote -v` shows `jlin3` in the URL.
